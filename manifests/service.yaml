apiVersion: v1
kind: Service
metadata:
  name: todo-ui
  namespace: default
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-resource-group: thakurapt6519
    service.beta.kubernetes.io/azure-pip-name: pip-dev-todoapp
  
spec:
  type: LoadBalancer
  selector:
    app: todo-ui
  ports:
    - port: 80
      targetPort: 80
